@import ('../../../../../libs/css/params.styl');

:host {
  width: 65%;
  margin-bottom: 3.64vw;
  position: relative;
  display: block;

  // height: 500px;
  .card-parent {
    width: 100%;
    box-sizing: border-box;
    padding: 0.25vw 1.57vw 1.3vw;
    position: relative;

    .number {
      position: absolute;
      font-weight: 500;
      color: white;
      top: 0;
      left: -2vw;
      fzd(20);
    }

    .header {
      fzd(28);
      font-weight: 400;
      color: white;
    }

    .about {
      fzd(20);
      lh(28);
      font-weight: 400;
      width: 97.5%;
      position: relative;
    }

    .buttons {
      margin-top: 2vw;
      fzd(18);
      color: white;
      display: flex;
      flex-direction: row;
      justify-content: flex-start;
      text-transform: uppercase;
      font-weight: 700;

      .choose {
        margin-right: 2vw;
      }
    }
  }
}
